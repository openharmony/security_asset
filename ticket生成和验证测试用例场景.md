| 用例类型 | 用例场景                                                     | 预期结果                                                     |
| -------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 基础     | osAccoutId：100；callerId："test_caller"；message列表长度：5；message列表中的message长度：5-100随机选 | 生成和验证成功                                               |
| 正向     | osAccoutId：101                                              | 生成和验证成功                                               |
| 正向     | message列表长度：99；且循环调用100次                         | 生成和验证成功                                               |
| 反向     | osAccoutId：0                                                | 生成失败；验证失败                                           |
| 反向     | osAccoutId：99                                               | 生成失败；验证失败                                           |
| 反向     | message列表长度：0                                           | 生成失败                                                     |
| 反向     | message列表长度：100                                         | 生成失败                                                     |
| 反向     | message列表中存在部分message长度为0                          | 生成和验证接口调用成功，但message为0的元素，生成的ticket也为0，且该元素会验证失败 |
| 反向     | message列表中存在所有message长度为0                          | 生成和验证接口调用成功，但message为0的元素，生成的ticket也为0，且该元素会验证失败 |
| 反向     | verifyInfo列表长度：0                                        | 验证失败                                                     |
| 反向     | verifyInfo列表长度：100                                      | 验证失败                                                     |
| 反向     | 生成后，将verifyInfo列表中其中2个元素的message互换后，验证   | 生成成功，验证接口调用成功，但被互换的元素会验证失败         |
| 反向     | 生成后，将verifyInfo列表中将其中2个元素的challenge互换后，验证 | 生成成功，验证接口调用成功，但被互换的元素会验证失败         |
| 反向     | 生成后，将verifyInfo列表中将其中2个元素的ticket互换后，验证  | 生成成功，验证接口调用成功，但被互换的元素会验证失败         |
| 反向     | 生成后，将verifyInfo列表中将其中1个元素的message篡改后，验证 | 生成成功，验证接口调用成功，但被篡改的元素会验证失败         |
| 反向     | 生成后，将verifyInfo列表中将其中1个元素的challenge长度改长后，验证 | 生成成功，验证接口调用成功，但被篡改的元素会验证失败         |
| 反向     | 生成后，将verifyInfo列表中将其中1个元素的challenge长度改短后，验证 | 生成成功，验证接口调用成功，但被篡改的元素会验证失败         |
| 反向     | 生成后，将verifyInfo列表中将其中1个元素的ticket长度改长后，验证 | 生成成功，验证接口调用成功，但被篡改的元素会验证失败         |
| 反向     | 生成后，将verifyInfo列表中将其中1个元素的ticket长度改短后，验证 | 生成成功，验证接口调用成功，但被篡改的元素会验证失败         |
| 反向     | 生成后，将verifyInfo列表中每个元素的message、challenge或ticket篡改后，验证 | 生成成功，验证接口调用成功，但被篡改的元素会验证失败         |

